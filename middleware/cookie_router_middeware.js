export default async ({ app }) => {
    console.log('>>>Router middeware cookie ....')
    // app.$cookiz.set('cookie-name', 'cookie-value', {
    //     path: '/',
    //     maxAge: 60 * 60 * 24 * 7
    // });
}